> selectProfile prod
> unmanagedResourceDirectories
> unmanagedSourceDirectories
> package
> run

# > showProfiles
# > unmanagedResourceDirectories

# > selectProfile prod
# > showProfiles
# > unmanagedResourceDirectories

# > package

$ pause
